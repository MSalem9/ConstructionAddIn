<UserControl x:Class="ConstructionAddIn.UserControls.Fittings.Reducer.ReducerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ConstructionAddIn.UserControls.Fittings.Reducer"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="90"
             d:DesignWidth="800">

    <UserControl.DataContext>
        <local:ReducerViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Light"
                                         PrimaryColor="Blue"
                                         SecondaryColor="Lime" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="FittingButtonTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="TextAlignment" Value="Center"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.5*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0"
                Margin="6,6,3,6"
                Height="82"
                Style="{StaticResource MaterialDesignOutlinedButton}"
                Command="{Binding DrawCommand}"
                CommandParameter="">
            <StackPanel Orientation="Vertical"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">

                <Canvas Width="42" Height="30" Margin="0,0,0,5">
                    <Polygon Fill="Black" Points="21,2 4,28 38,28"/>
                </Canvas>

                <TextBlock Text="Reducer"
                           Style="{StaticResource FittingButtonTextStyle}"/>
            </StackPanel>
        </Button>

        <Button Grid.Column="1"
                Margin="3,6,6,6"
                Height="82"
                Command="{Binding DeleteCommand}"
                CommandParameter=""
                Style="{StaticResource MaterialDesignRaisedButton}"
                Background="#D32F2F"
                Foreground="White"
                BorderBrush="#D32F2F">
            <materialDesign:PackIcon Kind="Delete" Width="24" Height="24"/>
        </Button>
    </Grid>
</UserControl>